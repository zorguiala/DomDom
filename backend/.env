# Application Settings
NODE_ENV=development
PORT=3001
API_PREFIX=/api

# Database Configuration - Prisma
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/domdom

# Database Configuration - Legacy (for TypeORM compatibility)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=domdom

# Authentication
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRATION=1d
JWT_REFRESH_SECRET=your_refresh_token_secret_key_change_this_in_production
JWT_REFRESH_EXPIRATION=7d

# Logging
LOG_LEVEL=debug

# CORS Settings
CORS_ORIGIN=http://localhost:5173

# File Storage
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880 # 5MB

# Email (if needed later)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASS=password

# Redis (if needed for caching later)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
